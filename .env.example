# 邮件通知配置示例（复制为 .env 并填入真实值，.env 已被 git 忽略，不会上传）
# 使用方式：复制本文件为 .env，然后填写下面几项。需要先安装：pip install python-dotenv

SMTP_USER=your_email@qq.com
SMTP_PASSWORD=你的邮箱授权码
NOTIFY_EMAIL=接收通知的邮箱@example.com

# 可选，QQ 邮箱可省略（已为默认）
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587
